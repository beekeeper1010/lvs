# LVS 本地视频播放服务 — 系统需求文档

> 🤖 **本项目由 AI 辅助开发**：代码实现与本文档均由 AI 协作完成。
> 版本：1.0.0
> 本文档描述"LVS 本地视频播放服务"的完整需求与设计，读者（包括人类开发者与 AI）依据本文档即可从零实现出功能一致的系统。

---

## 1. 系统概述

### 1.1 系统定位

一个部署在**本地主机**的私有视频播放服务。用户将 MP4 视频文件放在本地目录中，通过本系统自动扫描、生成缩略图、网页端浏览与播放。系统前端产物嵌入 Go 二进制，实现**单文件部署**，无需额外安装依赖（运行时仅需系统已安装 ffmpeg 用于扫描阶段）。

### 1.2 核心场景

1. 用户初始化系统，创建管理员账号
2. 用户将 MP4 文件放入本地目录，运行扫描命令，系统自动识别视频、探测时长、生成缩略图并入库
3. 用户通过浏览器登录，在"视频广场"分页浏览所有视频卡片
4. 用户点击卡片进入沉浸式播放页播放视频（支持拖拽进度条跳播）
5. 管理员可管理登录账号、删除视频库记录
6. 支持同一局域网内多设备访问

### 1.3 设计原则

- **单文件部署**：前端构建产物嵌入 Go 二进制，交付物仅一个可执行文件
- **无 CGO**：SQLite 使用纯 Go 驱动（`modernc.org/sqlite`），跨平台编译无需 C 工具链
- **开发阶段不做数据库迁移**：表结构变更时删除数据库重建（`lvs init` + `lvs scan`）

---

## 2. 技术栈

| 模块 | 技术 | 说明 |
| ---- | ---- | ---- |
| 后端 | Go + Gin | HTTP 服务框架 |
| 认证 | golang-jwt/jwt/v5 | JWT Token，HS256 签名 |
| 数据库 | SQLite（modernc.org/sqlite） | 纯 Go 驱动，无 CGO |
| 密码 | golang.org/x/crypto/bcrypt | 密码哈希存储 |
| 命令行 | spf13/cobra | 子命令解析 |
| 前端 | Vue3（组合式 API）+ Vite | SPA 应用 |
| 前端 UI | Element Plus | 组件库，深色主题 |
| 状态管理 | Pinia | 用户状态、广场分页状态 |
| 路由 | vue-router | 前端路由与守卫 |
| HTTP | axios | 请求封装与拦截器 |
| 视频处理 | ffmpeg / ffprobe | 缩略图提取、时长/流探测（仅扫描时依赖） |

---

## 3. 功能需求

### 3.1 命令行工具（CLI）

系统提供三个子命令：

| 命令 | 功能 | 参数 |
| ---- | ---- | ---- |
| `lvs init` | 初始化：创建 SQLite 数据库、建表、内置 admin 账号、生成 JWT secret | `-p` 管理员密码（必填）；`-d` 数据库路径（默认 `lvs.db`） |
| `lvs scan` | 递归扫描目录下所有 `.mp4`，探测时长、生成缩略图并入库 | `-D` 视频目录（必填）；`-d` 数据库；`-t` 缩略图目录（默认 `data/thumbs`） |
| `lvs serve` | 启动 HTTP 服务 | `-P` 监听端口（默认 8900）；`-d` 数据库路径 |

行为约束：

- 不带子命令直接运行 `lvs` 等价于 `lvs serve`
- `init` 时数据库文件已存在则报错退出；密码为空报错退出
- `scan` 时数据库不存在则报错退出（提示先 `lvs init`）

### 3.2 用户认证

- 用户名 + 密码登录，密码 bcrypt 校验
- 登录成功签发 JWT（有效期 24 小时），Claims 含：`uid`、`username`、`nickname`、`role`、`pwd_ver`
- 所有接口除登录外均需认证（`Authorization: Bearer <token>` 请求头）
- **query 传参兜底**：`<video>` 标签无法携带自定义请求头，播放/缩略图接口支持 `?token=xxx` 从 URL 参数取 token
- **密码版本机制（pwd_ver）**：修改密码后 `pwd_ver + 1`，旧 token 携带的 pwd_ver 与数据库不一致时判定失效，强制重新登录
- **错误码约定**：
  - 认证失败（未登录/token 过期/token 失效）→ HTTP 401
  - 业务错误（参数错误、无权限等）→ HTTP 200 + `{"code": 非0, "msg": "...", "data": null}`
  - 成功 → HTTP 200 + `{"code": 0, "msg": "ok", "data": ...}`

### 3.3 角色权限

| 角色 | 权限 |
| ---- | ---- |
| `admin` | 全部功能 + 用户管理（增删改查）、上传任意用户头像、删除视频库记录 |
| `user` | 登录、浏览广场、播放、修改自己昵称/密码/头像 |

- 内置账号：`admin`（init 时创建，角色 `admin`，昵称"管理员"）
- `admin` 账号不可删除；系统至少保留一个用户
- 前端路由守卫：非 admin 访问用户管理页重定向回广场

### 3.4 视频扫描与入库

- 递归扫描指定目录（含子目录）下所有 `.mp4` 文件
- 对每个视频执行一次 `ffprobe`（JSON 输出），探测：
  - 是否存在视频流（区分纯音频 mp4）
  - 视频时长（秒）：**优先取视频流时长，回退到 format.duration**
- 使用 `ffmpeg` 提取**视频中间时间点**的帧生成缩略图（宽 320，`scale=320:-2`，质量 3）
  - `-ss` 参数置于 `-i` 之前（快速 seek，避免全片解码）
  - 时长未知时回退提取首帧
  - 缩放参数失败时回退原尺寸输出（兼容旋转/奇数尺寸视频）
- **性能要求**：
  - 缩略图提取采用并发 worker，数量为 `min(CPU 核数, 8)`
  - 数据库写入串行执行（SQLite 单写者）
  - 已处理过的视频（记录存在且缩略图文件新于源文件 mtime）跳过，避免重复调用 ffprobe/ffmpeg
- **入库规则**：按 `path` 唯一键 upsert（INSERT OR UPDATE），更新 name/thumb_path/duration
- 纯音频 mp4（无视频流）：正常入库但缩略图为空，前端显示占位图标

### 3.5 视频广场

- 卡片式网格展示：缩略图 + 文件名 + 播放时长 + 点赞
- 卡片 meta 行（单行 flex 布局）：文件名（左，超长省略）→ 播放时长（等宽数字，格式 `HH:MM:SS`，不足 1 小时显示 `MM:SS`）→ 点赞按钮（拇指图标 + 计数，靠右）
- **点赞**：
  - 每个用户对同一视频最多点赞一次（数据库联合主键保证）
  - 点赞后计数 +1，再次点击取消点赞计数 -1
  - 已点赞状态用品牌色高亮标识
  - 点赞成功后以服务端返回的最新计数与状态更新本地数据（保证多端一致性）
- **分页浏览**：
  - 每页条数可选 `20 / 50 / 100`，默认 20
  - 切换每页条数后回到第 1 页
  - 翻页后平滑滚动回顶部
- **分页状态持久化**：页码与每页条数存入 Pinia store（内存），从播放页等其它页面返回广场时恢复原页码；注销时重置为第 1 页
- 卡片 hover 效果：上浮、阴影、缩略图微缩放、播放按钮浮现
- admin 用户卡片右上角显示删除按钮（hover 出现），删除前二次确认
- **回到顶部**：滚动超过 400px 时右下角显示"回到顶部"按钮（Teleport 到 body 保证 fixed 定位不被组件内 transform 动画破坏），点击平滑回顶

### 3.6 视频播放页

- 沉浸式影院风格：背景光晕、放映机光束、底部座位区光带
- `<video>` 原生播放器，`autoplay` + `playsinline`，支持 Range 分片（`http.ServeContent`），拖拽进度流畅
- 标题显示逻辑：
  - 播放开始后强制显示 3 秒（期间鼠标离开不隐藏）
  - 鼠标悬停播放器显示标题，移出隐藏
  - 标题悬停时以 tooltip 显示全名
- **上一集/下一集切换**：按视频 id 排序取前一个/后一个（`/api/video/adjacent`），左右箭头按钮（间距 118px），边界禁用；切换后替换当前路由并继续自动播放
- 右上角"← 返回广场"按钮，返回时保留广场分页状态

### 3.7 用户设置

- 修改昵称、修改密码（需校验当前密码，新密码二次确认，改密后强制重新登录）、上传头像
- 头像：
  - 保存到 `data/avatars/<username>.<ext>`，仅允许 `jpg/jpeg/png/webp/gif`，单文件 ≤ 2MB
  - 上传成功后清理该用户其它扩展名的旧头像文件
  - 数据库 `users.avatar` 存储相对路径
  - 无头像时前端显示昵称首字（大写）
  - 头像 URL 携带版本号参数，上传成功后递增，强制浏览器重新加载（配合 `Cache-Control: no-cache`）

### 3.8 用户管理（仅 admin）

- 用户列表表格：ID、头像、用户名、昵称、角色（标签）、创建时间
- 新增用户（用户名/昵称/密码，可选头像）
- 编辑用户（昵称、可选重置密码，用户名不可改；admin 账号删除按钮禁用）
- 删除用户（二次确认；admin 不可删；至少保留一个用户）
- 每个用户可单独设置头像

### 3.9 视频删除（仅 admin）

- 删除视频库记录与缩略图文件，**不删除源视频文件**（二次确认提示中明确说明）
- 删除后刷新当前页，若当前页已空且页码 > 1 则回退一页

### 3.10 注销

- 二次确认弹窗
- 注销后清除本地 token 与用户信息，重置广场分页状态，跳转登录页

---

## 4. 数据库设计

数据库：SQLite，单文件 `lvs.db`。连接数限制 `MaxOpenConns(1)`（SQLite 单写者）。

### 4.1 表 `users`

| 字段 | 类型 | 约束 | 说明 |
| ---- | ---- | ---- | ---- |
| id | INTEGER | PK AUTOINCREMENT | 用户 ID |
| username | TEXT | NOT NULL UNIQUE | 登录用户名 |
| password | TEXT | NOT NULL | bcrypt 哈希 |
| nickname | TEXT | NOT NULL DEFAULT '' | 显示昵称（空时回退用户名） |
| role | TEXT | NOT NULL DEFAULT 'user' | 角色：admin / user |
| pwd_ver | INTEGER | NOT NULL DEFAULT 0 | 密码版本（改密 +1，用于 token 失效） |
| avatar | TEXT | NOT NULL DEFAULT '' | 头像文件路径（空表示无） |
| created_at | DATETIME | NOT NULL | 创建时间 |

### 4.2 表 `videos`

| 字段 | 类型 | 约束 | 说明 |
| ---- | ---- | ---- | ---- |
| id | INTEGER | PK AUTOINCREMENT | 视频 ID |
| name | TEXT | NOT NULL | 文件名 |
| path | TEXT | NOT NULL UNIQUE | 源视频绝对路径（唯一键） |
| thumb_path | TEXT | NOT NULL DEFAULT '' | 缩略图路径（空表示无） |
| duration | REAL | NOT NULL DEFAULT 0 | 播放时长（秒） |
| like_count | INTEGER | NOT NULL DEFAULT 0 | 点赞总数 |
| created_at | DATETIME | NOT NULL | 入库时间 |

### 4.3 表 `video_likes`（点赞记录）

| 字段 | 类型 | 约束 | 说明 |
| ---- | ---- | ---- | ---- |
| video_id | INTEGER | PK（联合） | 视频 ID |
| user_id | INTEGER | PK（联合） | 点赞用户 ID |
| created_at | DATETIME | NOT NULL | 点赞时间 |

联合主键 `(video_id, user_id)` 在数据库层保证每个用户对同一视频最多点赞一次。

### 4.4 表 `settings`

| 字段 | 类型 | 约束 | 说明 |
| ---- | ---- | ---- | ---- |
| key | TEXT | PK | 配置键 |
| value | TEXT | NOT NULL | 配置值 |

目前仅一条：`('jwt_secret', <32字节随机hex>)`，用于 JWT 签名。JWT secret 在 `init` 时生成，服务启动时从数据库加载。

### 4.5 初始化流程

1. 创建数据库文件与三张表
2. bcrypt 哈希管理员密码，插入 admin 用户
3. 生成随机 32 字节 hex 作为 JWT secret 写入 settings

---

## 5. API 接口定义

### 5.1 通用约定

- 基础路径：`/api`
- 请求/响应编码：JSON（文件上传与下载除外）
- 统一响应：`{"code": 0, "msg": "ok", "data": {...}}`，`code=0` 成功
- 认证：`Authorization: Bearer <token>`；播放/缩略图支持 `?token=` 查询参数

### 5.2 接口清单

| 接口 | 方法 | 权限 | 说明 |
| ---- | ---- | ---- | ---- |
| `/api/login` | POST | 公开 | 请求 `{username, password}`；成功返回 `{token, username, nickname, role, avatar}` |
| `/api/logout` | POST | 认证 | 服务端仅返回成功（前端删除本地 token） |
| `/api/user/info` | GET | 认证 | 返回当前用户 `{username, nickname, role, avatar}`（昵称为空回退用户名） |
| `/api/user/profile` | PUT | 认证 | 请求 `{nickname?, password?, old_password?}`；改密码必须校验 `old_password`；昵称/密码至少一项 |
| `/api/user/avatar` | POST | 认证 | multipart 上传头像字段 `avatar` |
| `/api/user/avatar?username=x&token=...` | GET | 认证 | 返回头像图片；无头像返回 HTTP 204；响应头 `Cache-Control: no-cache` |
| `/api/video/list?page=1&pageSize=20` | GET | 认证 | 返回 `{list, total, page, pageSize}`；list 项 `{id, name, path, thumb_path, duration, like_count, liked}`（`liked` 为当前用户是否已点赞）；按 id 倒序；pageSize 上限 100 |
| `/api/video/play?id=1&token=...` | GET | 认证 | 视频流，`http.ServeContent` 支持 Range |
| `/api/video/thumb?id=1&token=...` | GET | 认证 | 返回缩略图图片；不存在返回业务错误 |
| `/api/video/adjacent?id=1` | GET | 认证 | 返回 `{prev, next}`，各为 `{id, name}` 或 null；按 id 排序取相邻 |
| `/api/video/:id/like` | PUT | 认证 | 点赞/取消点赞，请求 `{liked: true/false}`；事务内写入/删除点赞记录并重算 `like_count`，返回 `{like_count, liked}` |
| `/api/video/:id` | DELETE | admin | 删除视频记录 + 缩略图文件，保留源文件 |
| `/api/admin/users` | GET | admin | 返回全部用户列表 |
| `/api/admin/users` | POST | admin | 新增用户 `{username, password, nickname?}`；用户名重复报错；用户名 `admin` 自动设为管理员角色 |
| `/api/admin/users/:id` | PUT | admin | 编辑 `{nickname?, password?}`；改密码时 pwd_ver+1 |
| `/api/admin/users/:id` | DELETE | admin | 删除用户；admin 不可删；至少保留一个用户 |
| `/api/admin/users/:id/avatar` | POST | admin | 为指定用户上传头像（multipart 字段 `avatar`） |

### 5.3 认证中间件逻辑

1. 从 `Authorization: Bearer <token>` 取 token，为空则尝试 `?token=` 查询参数
2. 为空 → 401 `未登录`
3. JWT 解析失败（签名/过期）→ 401 `登录已过期, 请重新登录`
4. 查询用户 `pwd_ver`，与 token 中 `pwd_ver` 不一致（或用户不存在）→ 401 `登录已失效, 请重新登录`
5. 通过后 `claims` 存入 gin context，供后续 handler 使用

### 5.4 admin 中间件逻辑

- 从 context 取 claims，`role != "admin"` → HTTP 200 + code=2 `无权限` + Abort

---

## 6. 前端设计

### 6.1 工程结构

```text
web/
├── index.html             入口 HTML（含 favicon 引用）
├── vite.config.js         Vite 配置（/api 代理 → localhost:8900）
├── package.json           Vue3 + Vite + Element Plus + Pinia + vue-router + axios
├── public/
│   └── favicon.svg        站点图标（渐变圆角底 + 白色小播放三角）
└── src/
    ├── main.js            App 初始化：挂载 Pinia/Router/ElementPlus（中文 locale + 深色模式）
    ├── style.css          全局样式（CSS 变量、暗色主题变量、动画、滚动条）
    ├── api.js             axios 实例 + 拦截器 + 全部 API 封装
    ├── router.js          路由与全局守卫
    ├── stores/
    │   ├── user.js        Pinia：用户状态（token/username/nickname/role/avatar + localStorage 持久化）
    │   └── gallery.js     Pinia：广场分页状态（page/pageSize）
    ├── components/
    │   ├── Brand.vue      顶栏品牌区（渐变图标 + LVS 字标）
    │   └── PlayIcon.vue   播放三角图标（可设 size）
    └── views/
        ├── Login.vue      登录页
        ├── Gallery.vue    视频广场
        ├── Player.vue     播放页
        └── AdminUsers.vue 用户管理页
```

### 6.2 路由

| 路径 | 组件 | 守卫 |
| ---- | ---- | ---- |
| `/` | 重定向到 `/gallery` | — |
| `/login` | Login | 已登录访问则跳转广场 |
| `/gallery` | Gallery | 未登录跳登录 |
| `/play/:id` | Player | 未登录跳登录 |
| `/users` | AdminUsers | 未登录跳登录；非 admin 跳广场 |

守卫读取 Pinia 用户状态（初始化自 localStorage）。

### 6.3 axios 拦截器

- **请求拦截**：有 token 则自动附加 `Authorization: Bearer <token>`
- **响应拦截**：
  - 业务成功（`code === 0`）→ 直接返回 `data`
  - 业务失败（HTTP 200 + code≠0）→ reject `Error(msg)`（不跳转）
  - **HTTP 401** → 清空用户状态并 `location.href = '/login'`

### 6.4 全局样式要点

- 深色主题：背景 `#0b0c11`，卡片 `rgba(255,255,255,0.045)`，主色渐变 `#7c5cff → #22d3ee`
- 覆盖 Element Plus 暗色主题 CSS 变量（`html.dark`）
- 全局 `fadeUp` 入场动画、`spin` 旋转（加载 spinner）

### 6.5 各页面交互细节

**Login**：居中卡片，orb 光斑背景 + 网格底纹；品牌图标 + LVS 字标；用户名/密码输入，回车提交；错误/提示用 el-alert。

**Gallery**：

- 顶栏：Brand + 当前用户（头像/昵称）+ "操作"下拉（用户管理[admin]/用户设置/注销[分割线]）
- hero 区标题 + 总数
- 卡片网格（`minmax(230px,1fr)` 自适应），`v-loading` 加载态，空列表 el-empty 提示
- 底部 el-pagination：`layout="total, sizes, prev, pager, next"`，中文文案"共 N 条"、"N 条/页"
- 用户设置弹窗：头像上传 + 昵称 + 当前密码 + 新密码 + 确认新密码

**Player**：详见 3.6 节。

**AdminUsers**：表格 + 新增/编辑弹窗 + 删除确认。

### 6.6 用户状态同步

- 登录/注销时读写 localStorage（token/username/nickname/role/avatar）
- 页面加载时调 `/api/user/info` 同步最新昵称/头像（可能被其它端修改）
- 头像上传成功后：刷新用户信息 + 头像 URL 版本号 +1 强制重载

---

## 7. 关键实现细节（踩坑与约定）

1. **ffmpeg 取中间帧**：`-ss <duration/2>` 必须放在 `-i` **之前**，否则会全片解码（单视频耗时 >1s）。放前面用快速 seek 仅需毫秒级。
2. **时长探测**：`ffprobe -print_format json -show_entries stream=codec_type,duration:format=duration` 一次调用同时拿到"是否有视频流"和"时长"。优先视频流时长，缺失时回退 format.duration（部分视频 format 时长为 0）。
3. **纯音频 mp4**：无视频流则跳过缩略图，正常入库（thumb_path 为空）。
4. **缩略图入库丢记录 bug**：并发 worker 提取完缩略图后必须显式设置 `hasVideo`，否则入库时被当作无视频流处理。
5. **认证失败与业务错误区分**：认证失败必须用 HTTP 401（前端拦截器据此跳登录页）；业务错误用 HTTP 200 + code≠0，否则改密失败会误跳登录页。
6. **改密后旧 token 失效**：通过 `pwd_ver` 字段实现，比简单清除 token 更可靠。
7. **头像缓存刷新**：浏览器会缓存头像图片。通过"URL 版本号参数递增 + `Cache-Control: no-cache`"双重保障。
8. **`<video>` 无法带 Authorization 头**：播放/缩略图接口 token 走 query 参数。
9. **SQLite 并发**：`MaxOpenConns(1)`，写操作串行，避免 `database is locked`。
10. **开发阶段 schema 变更**：不做迁移，直接删除 lvs.db 重新 init + scan。
11. **无限滚动方案已回退**：曾实现 IntersectionObserver 无限滚动，因"返回广场后观察器不再触发"等交互问题，最终采用传统分页。如后续要做无限滚动，需在每次加载后兜底检查哨兵位置（`getBoundingClientRect`），不能只依赖 observer 状态变化。
12. **Element Plus 中文文案**：需配置 `locale: zhCn`（`element-plus/es/locale/lang/zh-cn`），否则分页显示 "Total"/"page" 英文。
13. **favicon 与页面图标一致性**：favicon 使用 SVG 内嵌渐变 + 白色播放三角，需在 index.html 用 `<link rel="icon">` 引用，且后端对 `/favicon.svg` 显式返回（避免被 SPA fallback 拦截）。

---

## 8. 部署与运行

### 8.1 环境要求

- 构建期：Go 1.25+、Node.js 18+
- 运行期：仅需可执行文件；`scan` 需系统 PATH 中有 ffmpeg/ffprobe

### 8.2 构建

```bash
cd web && npm install && npm run build   # 产物 web/dist
cd .. && go build -o lvs                 # Windows: lvs.exe
```

### 8.3 首次使用

```bash
./lvs init -p 123456        # 初始化数据库与 admin 账号
./lvs scan -D /path/to/videos  # 扫描视频
./lvs serve -P 8900         # 启动服务
```

### 8.4 数据文件

- `lvs.db`：SQLite 数据库
- `data/thumbs/`：缩略图（按视频路径 MD5 命名 `<md5>.jpg`）
- `data/avatars/`：用户头像（`<username>.<ext>`）

### 8.5 开发模式

```bash
./lvs serve              # 终端 1：后端 8900
cd web && npm run dev    # 终端 2：前端 5173（/api 代理到 8900）
```

---

## 9. 非功能需求

- **性能**：单视频缩略图提取 ≤ 数百毫秒（快速 seek）；扫描并发提升吞吐；分页接口响应 < 100ms（小数据集）
- **兼容性**：支持主流浏览器（Chrome/Edge/Firefox）；服务默认监听所有网卡，支持局域网访问
- **安全性**：bcrypt 密码哈希、JWT 签名、无 CGO 编译、头像类型与大小白名单
- **可维护性**：前后端分离的工程结构、统一响应格式、公共组件复用（Brand/PlayIcon）
